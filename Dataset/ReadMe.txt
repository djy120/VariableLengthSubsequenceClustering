For our method, we collet some open-source time series datasets and generate some synthetic time series.

The open-source time series we used are included in ReadData.m

Finally, all the open-source and synthetic time series are tranformed into ".mat" files and stored in "..\OurResults\*.mat" 